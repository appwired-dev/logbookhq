/**
 * Canonical icon map. Import icons from here, not from lucide-react directly,
 * so the set stays consistent and swaps are one-line. Default usage:
 *   <Icon.Plane size={16} strokeWidth={1.75} aria-hidden />
 */
export {
  // navigation
  LayoutDashboard, PlaneTakeoff, ChartSpline, FileBadge, ArrowLeftRight, Settings, ShieldCheck, Menu, X, Plus, Home, Ellipsis,
  // documents
  HeartPulse, Plane, CloudFog, GraduationCap, ScanFace, Map, Paperclip, FolderOpen,
  // status
  Check, CircleAlert, TriangleAlert, Info, PartyPopper,
  // sorting / misc
  ArrowUp, ArrowDown, ChevronsUpDown, ArrowRight, ArrowUpRight, ArrowDownRight, ChevronDown, Copy, Eye, EyeOff,
  LogOut, CreditCard, Languages, User, RotateCcw, Download, Upload, Clock, CalendarDays, Globe2, Search,
} from "lucide-react";
export type { LucideIcon } from "lucide-react";
